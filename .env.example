# Copy to .env and fill in. .env is gitignored; no real Steam ID belongs in this repo.

# The Steam ID naming your Brotato save directory:
#   ~/Library/Application Support/Brotato/<STEAM_ID>/save_v3_0.json
# Leave unset to have the save directory discovered by globbing that directory
# instead: the numeric directory wins over the empty `user/` profile Brotato
# writes beside it, so a normal install needs nothing set here.
# A 17-digit number; the value below is a placeholder, not an account.
STEAM_ID=00000000000000000

# Where Brotato keeps its per-player directories. Only set this if the game
# saves somewhere other than the path above — a non-default Steam library, say.
# BROTATO_APPLICATION_SUPPORT=
