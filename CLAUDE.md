## Agent skills

### Issue tracker

Issues live as GitHub issues in `xavxyz/brotato-coaching`, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary; each label string equals its role name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.

- Package boundaries are machine-checked: read `src/brotato_coaching/README.md` before adding a package, or importing across one.
